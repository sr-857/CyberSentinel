# 📦 Release: v1.0.0 — CyberSentinel MVP

## 🔍 Overview
CyberSentinel v1.0.0 delivers a complete SOC analyst workflow in a single deployable package. This release introduces automated threat intelligence ingestion, log correlation, and a real-time dashboard so defenders can triage suspicious activity quickly.

---

## 🆕 New Features
- ➕ Added AbuseIPDB and AlienVault OTX collectors with IOC normalization
- ➕ Added SSH and Apache log parsers with structured event extraction
- ➕ Added IOC correlation engine generating severity-tagged alerts
- ➕ Added responsive analyst dashboard with KPI tiles, charts, and data tables

---

## 🔧 Improvements
- Improved backend API layering with reusable services and SQLite persistence
- Refactored logging and environment configuration via `.env` handling
- Enhanced dashboard UX with Chart.js visualisations and KPI summaries

---

## 🐛 Bug Fixes
- Fixed parsing edge cases for SSH timestamps and Apache status codes
- Fixed frontend refresh workflow to sync alerts, logs, and analytics

---

## ⚠️ Breaking Changes
- None. Fresh installations only; future schema changes will be tracked here.

---

## 📦 Deployment Notes
- Copy `backend/.env.example` to `.env` and populate API keys before running
- Initialise the database if upgrading: `python -m backend.db`
- Rebuild containers after pulling: `docker-compose up --build`

---

## 📝 Full Changelog
Compare view: https://github.com/<your-username>/CyberSentinel/compare/HEAD~1...v1.0.0

---

## 🛡️ Security Notes
- API keys must remain in environment variables; `.env` is gitignored
- Input parsing validated for common injection vectors
- Logs and intel feeds mount read-only in Docker by default

---

## 🙌 Contributors
Thanks to:
- @<your-username> — initial architecture, implementation, and documentation

---

## 📑 License
This release is distributed under the MIT License. See `LICENSE` for details.
